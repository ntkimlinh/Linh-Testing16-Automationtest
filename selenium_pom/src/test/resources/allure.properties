#define nơi Allure ghi kết quả (json + attachement) sau mỗi lần chạy test
#define path mặc định target/allure-results vì đây là nơi Java mặc định lưu kq về
#lưu kết quả
allure.results.directory=target/allure-results
